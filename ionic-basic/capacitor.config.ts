import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicbasic031.app',
  appName: 'ionic-basic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
